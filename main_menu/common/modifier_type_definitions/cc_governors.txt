# Custom country modifier registering the count pool for the lesser ("frontier") local governor.
# Engine-internal num_local_governors / num_naval_governors are not declared in 00_modifier_types.txt,
# so our analogous count must be registered here to be readable via modifier:cc_num_frontier_governors.
# percent defaults to no -> plain additive integer count.
cc_num_frontier_governors = {
	game_data = {
		category = country
	}
}
