# INTENTIONALLY EMPTY.
#
# The per-bureau Taxis/Dynatoi drift that used to live here now sits in each bureaucracy's own
# positive_modifier, in in_game/common/bureaucracies/zzz_cc_byz_vanilla_overrides.txt. There
# it scales with funding, which an auto_modifier keyed on has_bureaucracy_of_type cannot do:
# from out here a bureau you have stopped paying pushes the axis exactly as hard as one you
# are funding properly.
#
# Also gone: an entrenchment tier that counted how many deeply entrenched bureaus a country
# ran and charged crown power for them. That was written before it was established that the
# ENGINE already scales every bureaucracy's boons and maluses by entrenchment on its own, so
# it was piling a third helping on top of a mechanic that already existed.
#
# Both were deleted rather than left in place because they would double count against the
# overrides.
#
# Kept as a file rather than deleted so the reasoning survives in the repo.
