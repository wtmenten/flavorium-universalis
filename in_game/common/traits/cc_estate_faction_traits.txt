
# ============================================================
# FLAVORIUM UNIVERSALIS — FAMILY G: ESTATE AFFILIATION TRAITS
# Pillar 02 — Estate-Aligned Cabinet Factions
#
# category = health (not cabinet) so these stack on top of a
# minister's main cabinet trait without occupying the slot.
# All direct modifiers are tiny (±0.05 or less) — the real
# gameplay impact comes from the events and auto-modifiers they
# gate, and the estate power amplification they enable.
# ============================================================

# --- Nobles ---
noble_champion_of_court = {
	category = health
	custom_tags = { estate_nobles }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = noble_champion_of_court }
	}

	modifier = {
		monthly_prestige = 0.05
		nobles_estate_agenda_impact = 0.04
	}
}

# --- Burghers ---
merchant_of_the_crown = {
	category = health
	custom_tags = { estate_burghers }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = merchant_of_the_crown }
	}

	modifier = {
		selling_efficiency = 0.03
		burghers_estate_agenda_impact = 0.04
	}
}

# --- Clergy ---
court_prelate = {
	category = health
	custom_tags = { estate_clergy }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = court_prelate }
	}

	modifier = {
		monthly_religious_influence = 0.05
		clergy_estate_agenda_impact = 0.04
	}
}

# --- Peasants ---
tribune_of_the_commons = {
	category = health
	custom_tags = { estate_peasants }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = tribune_of_the_commons }
	}

	modifier = {
		stability_decay = -0.0002
		monthly_rebel_growth = -0.001
	}
}

# --- Tribes (Nomadic/Steppe) ---
steppe_voice = {
	category = health
	custom_tags = { estate_tribes }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = steppe_voice }
	}

	modifier = {
		land_morale = 0.02
		army_light_cavalry_power = 0.02
	}
}

# --- Dhimmi ---
dhimmi_advocate = {
	category = health
	custom_tags = { estate_dhimmi }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = dhimmi_advocate }
	}

	modifier = {
		tolerance_heretic = 0.1
		diplomatic_reputation = 0.05
	}
}

# --- Cossacks ---
cossack_hetman_liaison = {
	category = health
	custom_tags = { estate_cossacks }
	flavor = personality

	allow = {
		in_cabinet = yes
		NOT = { has_trait = cossack_hetman_liaison }
	}

	modifier = {
		nobles_estate_agenda_impact = 0.03
		land_morale = 0.02
	}
}
