########################
# Cabinets and Choices — Stepping Stone Traits (Pillar 12)
# Tier 0 entry traits and chain endpoints for the progression system.
# Ministers advance through these chains via cc_progression_events.txt.
########################

########################
# TIER 0 — Entry Traits (mild negatives; the starting point, not the destination)
########################

# Chain A — The Reformer's Path
fumbling_reformist = {
	category = cabinet
	custom_tags = { administrative negative }
	flavor = personality

	allow = {
		NOT = { has_trait_category = cabinet }
	}

	modifier = {
		legislative_efficiency = -0.05
		stability_cost = 0.05
	}
}

# Chain C — The Diplomat's Ascent
tentative_envoy = {
	category = cabinet
	custom_tags = { diplomatic negative }
	flavor = personality

	allow = {
		NOT = { has_trait_category = cabinet }
	}

	modifier = {
		diplomatic_reputation = -0.5
		antagonism_received_modifier = 0.05
	}
}

# Chain D — The Military Hardening
green_adjutant = {
	category = cabinet
	custom_tags = { military negative }
	flavor = personality

	allow = {
		NOT = { has_trait_category = cabinet }
	}

	modifier = {
		discipline = -0.025
		land_morale_modifier = -0.05
	}
}

# Chain F — The Fiscal Path
clumsy_accountant = {
	category = cabinet
	custom_tags = { administrative negative }
	flavor = personality

	allow = {
		NOT = { has_trait_category = cabinet }
	}

	modifier = {
		court_spending_cost_modifier = 0.08
		global_monthly_prosperity = -0.005
	}
}


########################
# TIER 3+ — Chain Endpoints (awarded only by event after completing a full chain)
########################

# Chain A endpoint — master of administrative reform
master_reformer = {
	category = cabinet
	custom_tags = { administrative }
	flavor = personality

	allow = {
		has_variable = cc_chain_a_mastered
	}

	modifier = {
		legislative_efficiency = 0.10
		global_monthly_literacy = 0.01
		monthly_towards_innovative = societal_value_minor_monthly_move
		stability_cost = -0.05
	}
}

# Chain E endpoint — colonial veteran turned counsellor
returned_colonial_governor = {
	category = cabinet
	custom_tags = { diplomatic }
	flavor = personality

	allow = {
		has_variable = cc_chain_e_mastered
	}

	modifier = {
		colonial_migration_size_modifier = 0.15
		exploration_mission_speed = 0.10
		diplomatic_capacity = 1
		global_monthly_prosperity = 0.003
	}
}
